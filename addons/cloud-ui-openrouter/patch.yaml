apiVersion: apps/v1
kind: Deployment
spec:
  template:
    spec:
      containers:
        - name: ${CLOUD_UI_CONTAINER}
          env:
            - name: OPENROUTER_API_KEY
              valueFrom:
                secretKeyRef:
                  name: openrouter-api-key
                  key: api-key
