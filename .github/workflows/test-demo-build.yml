name: Demo Environment Build

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  test-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Install Helm
        uses: azure/setup-helm@1a275c3b69536ee54be43f2070a358922e12c8d4 # v4.3.1

      - name: Create Kind cluster
        uses: helm/kind-action@ef37e7f390d99f746eb8b610417061a60e82a6cc # v1.14.0
        with:
          config: kind-config.yaml
          cluster_name: toolhive-demo-in-a-box
          cloud_provider: true

      - name: Verify cluster is running
        run: |
          kubectl cluster-info
          kubectl get nodes

      - name: Install ToolHive CLI
        uses: StacklokLabs/toolhive-actions/install@9fb5f21a59fe9f9ffaba79ed0563997161acefc1 # v0.0.4

      - name: Install envsubst
        run: |
          sudo apt-get update
          sudo apt-get install -y gettext-base

      - name: Run bootstrap script
        run: |
          chmod +x bootstrap.sh
          ./bootstrap.sh
        env:
          # Enable debug mode for better CI logging
          DEBUG: "1"

      - name: Run validation script
        run: |
          chmod +x validate.sh
          ./validate.sh
